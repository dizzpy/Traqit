# Story 15.2: Add Security Headers Csp — Reliability & Defect Fixes

## Story

As a security-conscious product owner,
I want standard security headers and a CSP configured,
so that the app has baseline protection against common web vulnerabilities.

**Status:** ❌ Not started — verified against code on `main`, no implementation found.

## Acceptance Criteria

1. Given any page response, when inspected, then security headers (CSP, X-Frame-Options, X-Content-Type-Options, etc.) are present.

## Tasks / Subtasks

- [ ] Add headers config
  - [ ] Add a `headers()` function to `next.config.ts` with a reasonable CSP and standard security headers
  - [ ] Test that the CSP doesn't break existing third-party scripts/fonts already in use

## Dependencies

**Prerequisites:** None.
**Blocks:** None.

## Dev Notes

- `next.config.ts` currently sets no headers() function at all beyond image config. [Source: `current-state.md#Known, currently-open gaps`]

### Project Structure Notes

- Expected files:
  - `next.config.ts`

### Architecture Compliance

- Standard Next.js headers() config, no new dependency needed.

### Testing Requirements

- Load the app, confirm headers are present in the response and nothing (fonts, self-hosted assets, marketing animations) breaks.
