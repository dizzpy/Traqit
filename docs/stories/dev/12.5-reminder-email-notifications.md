# Story 12.5: Reminder Email Notifications — Settings, Profile & Notifications

## Story

As a job seeker with upcoming interviews,
I want to receive a reminder email before a scheduled pipeline stage,
so that I don't forget an upcoming interview.

## Acceptance Criteria

1. Given a pipeline stage with a scheduled date and the user's configured lead time (1/3/24/48 hours), when that lead time is reached, then a reminder email is sent.
2. Given a user has `remindersEnabled: false`, when the check runs, then no email is sent to them.
3. Given a reminder already sent for a stage, when the check runs again, then it is not sent a second time.

## Tasks / Subtasks

- [ ] Email infra (shared with 8.2)
  - [ ] Reuse the Resend + React Email setup from 8.2
  - [ ] Build a reminder-specific email template
- [ ] Scheduled check
  - [ ] Extend or add a `pg_cron` job checking upcoming PipelineStage.scheduledDate against each profile's reminderLeadTime
  - [ ] Mark stages as reminded to prevent duplicate sends

## Dependencies

**Prerequisites:** 3.3, 12.1
**Blocks:** 8.2

## Dev Notes

- `remindersEnabled` and `reminderLeadTime` are already fully wired on the data/UI side — this story is purely about building the actual sending mechanism that was missing. [Source: `traqit-srs.md §7 Known Gaps`]
- Cron frequency should be frequent enough to honor short lead times (e.g. 1 hour) meaningfully — a daily-only check would make short lead-time options misleading. Confirm final interval before implementation.

### Project Structure Notes

- Expected files:
  - `prisma/schema.prisma`
  - `src/lib/email.ts`
  - `src/app/api/cron/reminder-check/route.ts (new)`

### Architecture Compliance

- Same cron + email pattern as 8.2; likely worth combining both checks into a single scheduled route for efficiency.

### Testing Requirements

- Seed a stage with a near-future scheduled date and a short lead time, trigger the check, confirm exactly one email sends.
