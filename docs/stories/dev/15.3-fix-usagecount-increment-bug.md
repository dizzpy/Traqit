# Story 15.3: Fix Usagecount Increment Bug — Reliability & Defect Fixes

## Story

As a user relying on 'most used' sorting for Sources,
I want usageCount to increment correctly every time a source is actually used,
so that my dropdown ordering/insights reflect reality.

**Status:** ❌ Not started — verified against code on `main`, no implementation found.

## Acceptance Criteria

1. Given an application created with a Source but no pipeline template applied, when saved, then that Source's usageCount still increments.

## Tasks / Subtasks

- [ ] Fix the condition
  - [ ] In `applications/route.ts`, change the usageCount bump condition from `stageNames.length > 0` to check whether `appliedVia`/Source was actually supplied

## Dependencies

**Prerequisites:** 2.1, 9.1
**Blocks:** None.

## Dev Notes

- Currently gated on whether a pipeline template was applied, not on whether a source was actually supplied — looks copy-pasted from the wrong branch of logic. [Source: `current-state.md#Features §1`]

### Project Structure Notes

- Expected files:
  - `src/app/api/applications/route.ts`

### Architecture Compliance

- One-line logic fix, no schema change.

### Testing Requirements

- Create an application with a Source and no template, confirm usageCount increments.
