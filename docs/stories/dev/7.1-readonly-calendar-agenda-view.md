# Story 7.1: Readonly Calendar Agenda View — Calendar

## Story

As a job seeker,
I want a calendar view of all my upcoming interview stage dates,
so that I can see my schedule at a glance across all applications.

## Acceptance Criteria

1. Given pipeline stages with scheduled dates, when I open Calendar, then each appears on its correct day in the month grid.
2. Given the list view of the calendar, when toggled, then the same events appear in agenda/list form.

## Tasks / Subtasks

- [ ] Verify calendar rendering
  - [ ] Confirm month grid + list rendering in `calendar/page.tsx`

## Dependencies

**Prerequisites:** 3.3
**Blocks:** None.

## Dev Notes

- Display-only — no create/edit-from-calendar, no reminder delivery from this view, no external sync. [Source: `traqit-srs.md#FR-7.1/7.2`]

### Project Structure Notes

- Expected files:
  - `src/app/(main)/app/calendar/page.tsx`

### Architecture Compliance

- Builds CalEvents by scanning all applications' PipelineStage.scheduledDate client-side.

### Testing Requirements

- Set several stage dates across different applications, confirm all appear correctly positioned.
