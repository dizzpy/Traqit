# Story 8.2: Ghost Detection Email Notification — Ghost Detection

## Story

As a job seeker,
I want to receive an email when an application is detected as likely ghosted,
so that I don't have to keep checking the app manually to notice.

**Status:** ❌ Not started — verified against code on `main`, no implementation found.

## Acceptance Criteria

1. Given an application crosses the ghost threshold, when the scheduled check runs, then an email is sent to the user notifying them of the specific application.
2. Given a user has already been notified for a given application, when the check runs again, then they are not notified a second time for the same occurrence.
3. Given a user disables reminders/notifications in settings, when the check runs, then no email is sent to them.

## Tasks / Subtasks

- [ ] Email infra
  - [ ] Set up Resend account + API key (or SMTP credentials) as an env var/secret
  - [ ] Build a React Email template for the ghost-notification email
- [ ] Scheduled check
  - [ ] Add a `pg_cron` job (or extend the existing trash-purge pattern) to periodically call an API route
  - [ ] Route queries applications crossing the ghost threshold that haven't been notified yet
  - [ ] Mark as notified after sending, to prevent duplicates
- [ ] Opt-out
  - [ ] Respect the existing `remindersEnabled` profile setting before sending

## Dependencies

**Prerequisites:** 8.1, 12.5
**Blocks:** None.

## Dev Notes

- Uses Resend (free tier) + React Email + a Supabase pg_cron job calling a Next.js API route, reusing the existing trash-purge cron pattern. [Source: chat decision]
- Needs a 'notified' flag or timestamp on Application to prevent duplicate emails for the same ghost event — not currently in the schema.

### Project Structure Notes

- Expected files:
  - `prisma/schema.prisma`
  - `src/lib/email.ts`
  - `src/app/api/cron/ghost-check/route.ts (new)`

### Architecture Compliance

- Piggybacks on the same cron mechanism already proven for trash purge — no new infra pattern, just a new job + route.

### Testing Requirements

- Manually trigger the check route against a seeded ghost-eligible application, confirm exactly one email sends and a second run doesn't resend.
