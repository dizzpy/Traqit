# Story 15.4: Resolve Lint Failures Remove Dead Code — Reliability & Defect Fixes

## Story

As a developer maintaining this codebase,
I want lint to pass cleanly and dead code removed,
so that the codebase stays healthy and doesn't accumulate more cruft.

**Status:** ❌ Not started — verified against code on `main`, no implementation found.

## Acceptance Criteria

1. Given `bun run lint`, when run, then it completes with zero errors.
2. Given `src/lib/mock-data.ts`, when checked for imports, then it's either used somewhere or removed entirely.
3. Given `src/app/preview-landing/`, when its purpose is clarified, then it's either documented/linked properly or removed.

## Tasks / Subtasks

- [ ] Fix lint errors
  - [ ] Fix `react-hooks/refs` violations in `src/components/ui/particles.tsx:272-274` (move ref writes into an effect)
  - [ ] Remove the unused eslint-disable in `src/lib/prisma.ts:5`
- [ ] Dead code cleanup
  - [ ] Confirm zero imports of `mock-data.ts` repo-wide, then delete it
  - [ ] Clarify `preview-landing` purpose with product owner; remove or properly link it

## Dependencies

**Prerequisites:** None.
**Blocks:** 16.1, 16.2 (a clean lint baseline should exist before CI enforces it)

## Dev Notes

- This exact lint failure was flagged in a prior audit (June) and never fixed. [Source: `current-state.md#Known Issues`]

### Project Structure Notes

- Expected files:
  - `src/components/ui/particles.tsx`
  - `src/lib/prisma.ts`
  - `src/lib/mock-data.ts`
  - `src/app/preview-landing/page.tsx`

### Architecture Compliance

- No architectural change; pure cleanup.

### Testing Requirements

- Run `bun run lint` and confirm zero errors.
- Run `bun run build` to confirm no regression from the particles.tsx fix.
