# QA: Story 1.3 — Admin Allowlist Access Control — QA Verification

**Epic:** Auth & Authorization
**Traces to Dev Story:** 1.3-admin-allowlist-access-control.md

### Access Control
- [ ] Non-admin sees a standard 404 page, no indication the route exists.
- [ ] Admin (allowlisted email) sees the dashboard render correctly.
- [ ] Removing an email from ADMIN_EMAILS immediately revokes access (no session-cached bypass).

### Regression Sanity
- [ ] No console errors introduced in the browser during this flow.
- [ ] Data isolation holds — no other user's data is visible or affected.
- [ ] Feature behaves correctly on both desktop and a narrow (mobile-width) viewport, where applicable.
